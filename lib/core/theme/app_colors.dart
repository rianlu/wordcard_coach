import 'package:flutter/material.dart';

class AppColors {
  // 说明：逻辑说明
  static const Color primary = Color(0xFF257bf4); // 说明：逻辑说明
  static const Color secondary = Color(0xFFffcc00); // 说明：逻辑说明
  static const Color tertiary = Color(0xFFe91e63); // 说明：逻辑说明
  
  // 说明：逻辑说明
  static const Color background = Color(0xFFf5f7f8); // 说明：逻辑说明
  static const Color backgroundDark = Color(0xFF101722); // 说明：逻辑说明
  static const Color surface = Colors.white;

  // 说明：逻辑说明
  static const Color textHighEmphasis = Color(0xFF111418); // 说明：逻辑说明
  static const Color textMediumEmphasis = Color(0xFF60728a); // 说明：逻辑说明

  // 说明：逻辑说明
  static const Color shadowBlue = Color(0xFF1a5dbd);
  static const Color shadowYellow = Color(0xFFd4aa00);
  static const Color shadowWhite = Color(0xFFe2e8f0);
}
